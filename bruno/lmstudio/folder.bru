meta {
  name: LM Studio
  type: folder
}

docs {
  # LM Studio API Collection
  
  This folder contains Bruno requests for testing LM Studio's OpenAI-compatible API endpoints.
  
  ## Available Endpoints:
  - **Chat Completions**: Standard chat interface
  - **Chat Completions (with schema)**: Structured output with JSON schema
  - **List Models**: Get available models
  
  ## Features:
  - Native OpenAI API compatibility
  - Superior structured output quality via constrained generation
  - Grammar-based sampling for GGUF models
  - Outlines library for MLX models
}
