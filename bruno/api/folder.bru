meta {
  name: API
  seq: 4
}

auth {
  mode: basic
}

auth:basic {
  username: {{auth_username}}
  password: {{auth_password}}
}

docs {
  # vLLM API Collection

  OpenAI-compatible API endpoints proxied through Nginx + Fastify.

  ## Available Endpoints:
  - **Chat Completions**: Standard chat interface
  - **Chat Completions (with schema)**: Structured output with JSON schema
  - **List Models**: Get available models
}
