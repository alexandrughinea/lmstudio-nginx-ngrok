meta {
  name: Authentication Test
  type: http
  seq: 2
}

get {
  url: {{base_url}}/health
  body: none
  auth: basic
}

headers {
  Content-Type: application/json
}

auth:basic {
  username: {{auth_username}}
  password: {{auth_password}}
}

docs {
  # Authentication Test
  
  Tests basic authentication against the LM Studio proxy.
  This endpoint should return a 200 OK status with authentication.
}
