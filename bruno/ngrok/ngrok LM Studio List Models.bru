meta {
  name: ngrok LM Studio List Models
  type: http
  seq: 3
}

get {
  url: {{ngrok_url}}/v1/models
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

docs {
  # ngrok LM Studio List Models
  
  Lists all available models in LM Studio through ngrok tunnel.
  Returns OpenAI-compatible model list format.
}
