meta {
  name: ngrok LM Studio Chat Completions
  type: http
  seq: 5
}

post {
  url: {{ngrok_url}}/v1/chat/completions
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "id": "test-external-id-alex",
    "model": "gemma-3-27b-it",
    "messages": [
      {
        "role": "user",
        "content": "Hello! How are you today?"
      }
    ],
    "temperature": 0.7,
    "max_tokens": 150,
    "stream": false
  }
}

docs {
  # ngrok LM Studio Chat Completions
  
  Test LM Studio chat completions through the public ngrok tunnel.
  Uses the same OpenAI-compatible API as the local endpoint.
}
